<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.SurveyTargetDao">
	<sql id="key">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="id != null">
				id,
			</if>
			<if test="userId != null">
				user_id,
			</if>
			<if test="surveyId != null">
				survey_id,
			</if>
			<if test="orgId != null">
				org_id,
			</if>
			<if test="gmtCreate != null">
				gmt_create,
			</if>
		</trim>
	</sql>
	<!-- sql片段对应?,id属性值任意 -->
	<sql id="value">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="id != null">
				#{id},
			</if>
			<if test="userId != null">
				#{userId},
			</if>
			<if test="surveyId != null">
				#{surveyId},
			</if>
			<if test="orgId != null">
				#{orgId},
			</if>
			<if test="gmtCreate != null">
				#{gmtCreate},
			</if>
		</trim>
	</sql>
	<!-- 新增一个SurveySurveyTarget -->
	<insert id="addSurveyTarget" parameterType="SurveySurveyTarget">
		<selectKey resultType="java.lang.Long" order="AFTER"
			keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句 BEFORE 或 AFTER -->
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into survey_target (
		<include refid="key" />
		)
		values(
		<include refid="value" />
		);
	</insert>
	<!-- 删除SurveyTarget通过主键id -->
	<delete id="deleteSurveyTargetById" parameterType="java.lang.Long">
		delete from survey_target where id=#{id};
	</delete>
	<!-- 更新一个 SurveyTarget通过主键id -->
	<update id="updateSurveyTarget" parameterType="SurveyTarget">
		update survey_target
		<set>
			<if test="id != null">
				id=#{id},
			</if>
			<if test="userId != null">
				user_id=#{userId},
			</if>
			<if test="surveyId != null">
				survey_id=#{surveyId},
			</if>
			<if test="orgId != null">
				org_id=#{orgId},
			</if>
			<if test="gmtCreate != null">
				gmt_create=#{gmtCreate},
			</if>
		</set>
		where
		id=#{id} ;
	</update>
	<!-- 查找SurveyTarget通过主键id -->
	<select id="selectSurveyTargetById" parameterType="java.lang.Long" 
	resultType="SurveyTarget">
		select * from survey_target where id=#{id};
	</select>
</mapper>