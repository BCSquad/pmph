<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.OrgMessageDao">

	<!-- 新增 OrgMessage -->
    <insert id="addOrgMessage"  parameterType="OrgMessage"  useGeneratedKeys="true" keyProperty="id"  >
    	<selectKey resultType="java.lang.Long" order="AFTER" keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句  BEFORE 或 AFTER -->
          SELECT LAST_INSERT_ID() 
        </selectKey>
    	insert  into  org_message (msg_code,msg_type)  values(#{msgCode,jdbcType=VARCHAR},#{msgType,jdbcType=TINYINT}) ;
    </insert>
    
   <!-- 根据id删除一个 -->
	<delete id="deleteOrgMessageById" parameterType="OrgMessage"   >
		delete from  org_message  where id=#{id} ;
	</delete>
	
	<!-- 更新OrgMessage -->
	<update id="updateOrgMessageById"   parameterType="OrgMessage"  >
		update org_message   set   msg_code=#{msgCode} , msg_type=#{msgType}  where id=#{id} ;
	</update>
	
	<!-- 根据id查询一个OrgMessage -->
	<select  id="getOrgMessageById" parameterType="OrgMessage"  resultType="OrgMessage" >
		select * from org_message where id=#{id} ;
	</select>
	
</mapper>