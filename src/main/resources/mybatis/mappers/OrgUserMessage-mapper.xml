<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.OrgUserMessageDao">

	<sql id="key">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="msgId != null and msgId !=''">
				msg_id,
			</if>
			<if test="userId != null and userId !=''">
				user_id,
			</if>
			<if test=" isRead != null">
				is_read,
			</if>
			<if test=" isDeleted != null ">
				is_deleted,
			</if>
		</trim>
	</sql>
	<!-- sql片段对应?,id属性值任意 -->
	<sql id="value">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="msgId != null and msgId !=''">
				#{msgId},
			</if>
			<if test="userId != null and userId !=''">
				#{userId},
			</if>
			<if test=" isRead != null ">
				#{isRead},
			</if>
			<if test=" isDeleted != null ">
				#{isDeleted},
			</if>
		</trim>
	</sql>

	<!-- 新增 OrgUserMessage -->
	<insert id="addOrgUserMessage" parameterType="OrgUserMessage">
		<selectKey resultType="java.lang.Long" order="AFTER"
			keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句 BEFORE 或 AFTER -->
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into org_user_message (msg_id ,user_id,is_read,is_deleted)
		values(#{msgId,jdbcType=BIGINT},
		#{userId,jdbcType=BIGINT},
		#{isRead,jdbcType=TINYINT},
		#{isDeleted,jdbcType=TINYINT}) ;
	</insert>

	<!-- 根据id删除一个 OrgUserMessage -->
	<delete id="deleteOrgUserMessageById" parameterType="java.lang.Long">
		delete from
		org_user_message where id=#{id} ;
	</delete>

	<!-- 更新 OrgUserMessage -->
	<update id="updateOrgUserMessage" parameterType="OrgUserMessage">
		update org_user_message
		<set>
			<if test="msgId != null and msgId !=''">
				msg_id = #{msgId},
			</if>
			<if test="userId != null and userId !='' ">
				user_id = #{userId},
			</if>
			<if test=" isRead != null">
				is_read= #{isRead},
			</if>
			<if test=" isDeleted != null">
				is_deleted= #{isDeleted},
			</if>
		</set>
		where id=#{id} ;
	</update>

	<!-- 根据id查询一个 OrgUserMessage -->
	<select id="getOrgUserMessageById" parameterType="java.lang.Long"
		resultType="OrgUserMessage">
		select * from org_user_message where
		id=#{id} ;
	</select>

	<!-- 查询表单的数据总数 -->
	<select id="getOrgUserMessageCount" resultType="Long">
		select
		count(*)
		from
		org_user_message
	</select>

</mapper>