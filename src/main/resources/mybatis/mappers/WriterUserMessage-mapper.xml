<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.WriterUserMessageDao">

	<sql id="key">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="msgId != null and msgId !=''">
				msg_id,
			</if>
			<if test="userId != null and userId !=''">
				user_id,
			</if>
			<if test="isRead != null">
				is_read,
			</if>
			<if test="isDeleted != null">
				is_deleted,
			</if>
		</trim>
	</sql>
	<!-- sql片段对应?,id属性值任意 -->
	<sql id="value">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="msgId != null and msgId !=''">
				#{msgId},
			</if>
			<if test="userId != null and userId !=''">
				#{userId},
			</if>
			<if test="isRead != null ">
				#{isRead},
			</if>
			<if test="isDeleted != null">
				#{isDeleted},
			</if>
		</trim>
	</sql>

	<!-- 新增 WriterUserMessage -->
	<insert id="addWriterUserMessage" parameterType="WriterUserMessage">
		<selectKey resultType="java.lang.Long" order="AFTER"
			keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句 BEFORE 或 AFTER -->
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into writer_user_message (
		<include refid="key" />
		)
		values(
		<include refid="value" />
		) ;
	</insert>

	<!-- 根据id删除一个 WriterUserMessage -->
	<delete id="deleteWriterUserMessageById" parameterType="java.lang.Long">
		delete
		from writer_user_message where id=#{id} ;
	</delete>

	<!-- 更新 WriterUserMessage -->
	<update id="updateWriterUserMessage" parameterType="WriterUserMessage">
		update writer_user_message
		<set>
			<if test="msgId !=null and msgId!='' ">
				msg_id=#{msgId},
			</if>
			<if test="userId!=null and userId!=''">
				user_id=#{userId},
			</if>
			<if test="isRead!=null">
				is_read=#{isRead},
			</if>
			<if test="isDeleted!=null">
				is_deleted=#{isDeleted}
			</if>
		</set>
		where id=#{id} ;
	</update>

	<!-- 根据id查询一个 WriterUserMessage -->
	<select id="getWriterUserMessageById" parameterType="java.lang.Long"
		resultType="WriterUserMessage">
		select * from writer_user_message where
		id=#{id} ;
	</select>

	<!-- 查询表单的数据总条数 -->
	<select id="getWriterUserMessageCount" resultType="java.lang.Long">
		select
		count(*) from writer_user_message ;
	</select>
</mapper>