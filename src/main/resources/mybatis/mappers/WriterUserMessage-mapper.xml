<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.WriterUserMessageDao">

	<!-- 新增 WriterUserMessage -->
	<insert id="addWriterUserMessage" parameterType="WriterUserMessage">
		<selectKey resultType="java.lang.Long" order="AFTER"
			keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句 BEFORE 或 AFTER -->
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into writer_user_message (msg_id, user_id,is_read, is_deleted)
		values(#{msgId,jdbcType=BIGINT},#{userId,jdbcType=BIGINT},#{isRead,jdbcType=TINYINT},#{isDelete,jdbcType=TINYINT})
		;
	</insert>

	<!-- 根据id删除一个 WriterUserMessage -->
	<delete id="deleteWriterUserMessageById" parameterType="WriterUserMessage">
		delete
		from writer_user_message where id=#{id} ;
	</delete>

	<!-- 更新 WriterUserMessage -->
	<update id="updateWriterUserMessageById" parameterType="WriterUserMessage">
		update writer_user_message
		<set>
			<if test="msgId !=null and msgId!='' ">
				msg_id=#{msgId},
			</if>
			<if test="userId!=null and userId!=''">
				user_id=#{userId},
			</if>
			<if test="isRead!=null">
				is_read=#{isRead},
			</if>
			<if test="isDeleted!=null">
				is_deleted=#{isDeleted}
			</if>
		</set>
		where id=#{id} ;
	</update>

	<!-- 根据id查询一个 WriterUserMessage -->
	<select id="getWriterUserMessageById" parameterType="WriterUserMessage"
		resultType="WriterUserMessage">
		select * from writer_user_message where
		id=#{id} ;
	</select>

</mapper>