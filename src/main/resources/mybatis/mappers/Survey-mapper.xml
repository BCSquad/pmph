<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmpheep.back.dao.SurveyDao">
	<sql id="key">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="id != null">
				id,
			</if>
			<if test="title != null and title !=''">
				title,
			</if>
			<if test="subhead != null and subhead !=''">
				subhead,
			</if>
			<if test="intro != null and intro !=''">
				intro,
			</if>
			<if test="templateId != null">
				template_id,
			</if>
			<if test="typeId != null">
				type_id,
			</if>
			<if test="userId != null">
				user_id,
			</if>
			<if test="beginDate != null and beginDate !=''">
				begin_date,
			</if>
			<if test="endDate != null and endDate !=''">
				end_date,
			</if>
			<if test="sort != null">
				sort,
			</if>
			<if test="isDeleted != null and isDeleted !=''">
				is_deleted,
			</if>
			<if test="gmtCreate != null and gmtCreate !=''">
				gmt_create,
			</if>
			<if test="gmtUpdate != null and gmtUpdate !=''">
				gmt_update,
			</if>
		</trim>
	</sql>
	<!-- sql片段对应?,id属性值任意 -->
	<sql id="value">
		<!-- 去掉最后一个, -->
		<trim suffixOverrides=",">
			<if test="id != null">
				#{id},
			</if>
			<if test="title != null and title !=''">
				#{title},
			</if>
			<if test="subhead != null and subhead !=''">
				#{subhead},
			</if>
			<if test="intro != null and intro !=''">
				#{intro},
			</if>
			<if test="templateId != null">
				#{templateId},
			</if>
			<if test="typeId != null">
				#{typeId},
			</if>
			<if test="userId != null">
				#{userId},
			</if>
			<if test="beginDate != null and beginDate !=''">
				#{beginDate},
			</if>
			<if test="endDate != null and endDate !=''">
				#{endDate},
			</if>
			<if test="sort != null">
				#{sort},
			</if>
			<if test="isDeleted != null and isDeleted !=''">
				#{isDeleted},
			</if>
			<if test="gmtCreate != null and gmtCreate !=''">
				#{gmtCreate},
			</if>
			<if test="gmtUpdate != null and gmtUpdate !=''">
				#{gmtUpdate},
			</if>
		</trim>
	</sql>
	<!-- 新增一个Survey -->
	<insert id="addSurvey" parameterType="Survey">
		<selectKey resultType="java.lang.Long" order="AFTER"
			keyProperty="id"> <!-- order="AFTER"表示先执行插入语句，之后再执行查询语句 BEFORE 或 AFTER -->
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into survey (
		<include refid="key" />
		)
		values(
		<include refid="value" />
		);
	</insert>
	<!-- 删除Survey通过主键id -->
	<delete id="deleteSurveyById" parameterType="java.lang.Long">
		delete from survey where id=#{id};
	</delete>
	<!-- 更新一个 SurveyQuestionnaire通过主键id -->
	<update id="updateSurvey" parameterType="Survey">
		update survey
		<set>
			<if test="id != null">
				id=#{id},
			</if>
			<if test="title != null and title !=''">
				title=#{title},
			</if>
			<if test="subhead != null and subhead !=''">
				subhead=#{subhead},
			</if>
			<if test="intro != null and intro !=''">
				intro=#{intro},
			</if>
			<if test="templateId != null">
				template_id=#{templateId},
			</if>
			<if test="typeId != null">
				type_id=#{typeId},
			</if>
			<if test="userId != null">
				user_id=#{userId},
			</if>
			<if test="beginDate != null and beginDate !=''">
				begin_date=#{beginDate},
			</if>
			<if test="endDate != null and endDate !=''">
				end_date=#{endDate},
			</if>
			<if test="sort != null">
				sort=#{sort},
			</if>
			<if test="isDeleted != null and isDeleted !=''">
				is_deleted=#{isDeleted},
			</if>
			<if test="gmtCreate != null and gmtCreate !=''">
				gmt_create=#{gmtCreate},
			</if>
			<if test="gmtUpdate != null and gmtUpdate !=''">
				gmt_update=#{gmtUpdate},
			</if>
		</set>
		where
		id=#{id};
	</update>
	<!-- 查找Survey通过主键id -->
	<select id="selectSurveyById" parameterType="java.lang.Long" resultType="Survey">
		select * from survey where id=#{id};
	</select>
</mapper>